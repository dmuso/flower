version: "2"
linters:
  default: none
  enable:
    - errcheck
    - govet
    - ineffassign
    - staticcheck
    - unused
    - revive
    - misspell
  settings:
    misspell:
      locale: UK
    revive:
      rules:
        - name: exported
          disabled: true
run:
  timeout: 5m
