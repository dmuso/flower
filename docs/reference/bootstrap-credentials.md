# Bootstrap credentials

Local development uses the values in `.env.example`. There is no seeded admin user yet; authentication will be added as a product feature.

Until then, the API is reachable without login on `/health`, `/ready`, and `/api/version`.
